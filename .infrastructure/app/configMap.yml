apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-sql
  namespace: mysql
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS todo;
    GRANT ALL PRIVILEGES ON todo.* TO 'appuser'@'%' IDENTIFIED BY 'apppass';
    FLUSH PRIVILEGES;
